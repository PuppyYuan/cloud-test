spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/puppyyuan/spring-cloud-config-repo
          username:
          password:
server:
  port: 12000